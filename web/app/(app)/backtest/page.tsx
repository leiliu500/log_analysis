// Backtest is a tab on the main workspace (/); this route keeps the standalone
// /backtest URL working as a deep link, rendering the same view component.
export { BacktestView as default } from '@/components/BacktestView';
