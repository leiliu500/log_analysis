// Validation is a tab on the main workspace (/); this route keeps the standalone
// /validation URL working as a deep link, rendering the same view component.
export { ValidationView as default } from '@/components/ValidationView';
